name: test Hello Action
on:
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - uses: lgmorand/github-action-hello@v2.x
        with:
          firstname: jean
          firstname2: marie
