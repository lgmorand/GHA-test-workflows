name: Dual jobs
on:
  workflow_dispatch:

jobs:
  job1:
    runs-on: self-hosted
    steps:
      - run: echo "hello job2" > test.txt
  
  job2:
    runs-on: self-hosted
    needs: job1
    steps:
      - run: ls
      - run: cat test.txt
